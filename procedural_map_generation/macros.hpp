#ifndef MACROS_HPP
#define MACROS_HPP

constexpr int WIDTH = 1920;
constexpr int HEIGHT = 1080;
constexpr int CELL_SIZE = 2;
constexpr int ROWS = HEIGHT / CELL_SIZE;
constexpr int COLS = WIDTH / CELL_SIZE;
constexpr int MAX_SIZE = ROWS * COLS;

#endif